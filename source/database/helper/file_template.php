<?php
  include_once "../Studying/helper/db/db.php";

  $database = "";
  
  $sql = "";
  $data = get_data_query($sql);

  function name() {
    global $data;

    foreach ($data as $line) {
      
    }
  }

  run($result, "");
?>